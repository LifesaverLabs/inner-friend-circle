// Semantic version for the application
// Update this when releasing new versions
export const APP_VERSION = "0.1.10";
export const VERSION_TAG = "alpha-unstable";
export const FULL_VERSION = `v${APP_VERSION}-${VERSION_TAG}`;
